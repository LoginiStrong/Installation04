#pragma once
#include <string>
#include <vector>
using namespace std;

//Skeleton of the function that builds the string that makes up the current board
string getboard(int pos, vector<int> & obstacles);
